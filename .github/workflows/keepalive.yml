name: Keepalive

on:
  schedule:
    - cron: '0 12 1 * *'  # 1. dnia kazdego miesiaca - zapobiega dezaktywacji
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Keepalive ping - $(date)"
